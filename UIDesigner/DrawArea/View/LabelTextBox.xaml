<UserControl x:Class="DrawArea.View.LabelTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignWidth="40" Height="20">
    <DockPanel Margin="0,0,0,0">
        <TextBlock Name="txb" Text="{Binding Path=Label}"
                 HorizontalAlignment="Right" Width="20"/>
        <TextBox Name="txt" Text="{Binding Path=Value}"
                 HorizontalAlignment="Left" Width="20"/>
    </DockPanel>
</UserControl>
